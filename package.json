{
  "name": "penG-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "PenG frontend — static JS for mindmap and quiz rendering",
  "scripts": {
    "build": "echo 'No build step required; static files served directly by FastAPI'",
    "dev": "npx live-server static/ --port=3000 --proxy=/api:http://localhost:8000/api"
  },
  "dependencies": {
    "markmap-lib": "^0.18.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-quiz-component": "^1.1.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
